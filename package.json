{
  "name": "Portfolio",
  "description": "",
  "scripts": {
    "scss": "node-sass --source-map-embed src/scss/index.scss dist/main.css && npm run postcss",
    "postcss": "postcss -u autoprefixer -r dist/main.css",
    "watch:html": "onchange 'src/index.html' -- cp src/index.html dist",
    "watch:css": "onchange 'src/scss/*.scss' -i -- npm run scss",
    "watch": "npm run watch:html & npm run watch:css",
    "start": "npm run dist && npm run watch & live-server dist",
    "build:css": "npm run scss && csso dist/main.css dist/main.css",
    "images": "imagemin src/images/* -o=dist/images",
    "predist": "rm -rf dist && mkdir -p dist",
    "dist": "cp -t dist src/CNAME src/favicon.ico src/index.html src/resume.pdf && npm run images",
    "build": "npm run dist && npm run build:css"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.5",
    "csso-cli": "^1.1.0",
    "imagemin-cli": "^3.0.0",
    "live-server": "^1.2.0",
    "node-sass": "^4.5.3",
    "onchange": "^3.2.1",
    "postcss-cli": "^4.1.1"
  },
  "browserslist": [
    "last 2 versions",
    "IE >= 11"
  ]
}
